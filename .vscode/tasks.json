{
   "version": "2.0.0",
   "tasks": [
      {
         "label": "Start Development Server",
         "type": "shell",
         "command": "cd frontend && npm run dev",
         "options": {
            "cwd": "${workspaceFolder}"
         }
      }
   ]
}
